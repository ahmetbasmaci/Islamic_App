enum ZikrCategories {
  morning,
  evening,
  wakeUp,
  sleep,
  travel,
  eating,
  mosque,
  home,
  toilet,
  haj,
  allahNames,
}

enum ALarmPeriod { daily, weekly, monthly, once, repeat }

enum RepeatAlarmType { high, medium, low, rare, none }

enum ALarmType {
  dua,
  hadith,
  dailyAzkar,
  quran,
  fast,
  pray,
}
